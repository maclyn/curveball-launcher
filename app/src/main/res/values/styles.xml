<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light"></style>

    <!-- Settings theme -->
    <style name="SettingsTheme" parent="Theme.AppCompat"></style>

    <style name="AppThemeDarkNoActionBar" parent="Theme.AppCompat.NoActionBar" />

    <style name="AppThemeNoActionBar" parent="Theme.AppCompat.NoActionBar" />

    <style name="HomeTheme" parent="AppThemeNoActionBar">
        <!-- Show wallpaper behind activity -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowShowWallpaper">true</item>

        <!-- Translucent status bar/navigation bar -->
        <item name="android:statusBarColor">@color/transparent</item>
        <item name="android:navigationBarColor">@color/transparent</item>

        <item name="android:windowEnterTransition">@transition/activity_transition</item>
        <item name="android:windowExitTransition">@transition/activity_transition</item>
    </style>

    <style name="HomeThemeAppCompatSource" parent="Theme.AppCompat">
        <!-- Show wallpaper behind activity -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:colorBackgroundCacheHint">@null</item>
        <item name="android:windowShowWallpaper">true</item>

        <!-- Translucent status bar/navigation bar -->
        <!--
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowDrawsSystemBarBackgrounds">false</item>
        -->

        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="BylineStyle">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">@dimen/byline_text_size</item>
        <item name="android:lineSpacingMultiplier">1.2</item>
    </style>

    <style name="BylineStyle.Bolded">
        <item name="android:clickable">true</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="BoldedText">
        <item name="android:textStyle">bold</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="BottomSheetSettingStyle">
        <item name="android:background">?attr/selectableItemBackground</item>
        <item name="android:clickable">true</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingStart">16dp</item>
        <item name="android:paddingEnd">16dp</item>
    </style>
</resources>
